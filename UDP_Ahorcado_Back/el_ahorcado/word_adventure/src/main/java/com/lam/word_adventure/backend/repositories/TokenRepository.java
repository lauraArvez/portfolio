/*package com.lam.word_adventure.backend.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Component;

import com.lam.word_adventure.backend.models.TokenModel;

/**
 * interface repository de Token
 * @author Laura Arvez
 */
/*@Component
public interface TokenRepository extends JpaRepository<TokenModel, Long> {
    /**
     * busca un token
     * @param token
     * @return boolean
     */
   /* Boolean existsByToken(String token);
}*/
